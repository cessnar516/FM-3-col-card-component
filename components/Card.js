class Card {
    constructor(
        id,
        image,
        name,
        text,
        link
    ) {
        this.id = id;
        this.image = image;
        this.name = name;
        this.text = text;
        this.link = link;
    }
}

export default Card;